apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: proxy
  namespace: project1
  annotations:
    ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - http:
      paths:
        - path: /
          backend:
            serviceName: nginx
            servicePort: 80
